import { LocalStorageKeys } from "@/types/local-storage.types";

const LOCAL_STORAGE: Record<LocalStorageKeys, LocalStorageKeys> = {
  accessTokenKey: "accessTokenKey",
  lang: "lang",
};

export default LOCAL_STORAGE;
